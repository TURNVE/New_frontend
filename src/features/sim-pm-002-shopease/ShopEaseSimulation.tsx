import SimulationShell from '../../shared/simulation/SimulationShell';
import { SHOPEASE_CONFIG } from './config';

export default function ShopEaseSimulation() {
    return <SimulationShell config={SHOPEASE_CONFIG} />;
}
